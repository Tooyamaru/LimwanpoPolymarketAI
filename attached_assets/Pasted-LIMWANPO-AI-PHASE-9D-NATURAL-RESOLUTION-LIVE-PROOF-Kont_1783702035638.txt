LIMWANPO AI — PHASE 9D NATURAL RESOLUTION LIVE PROOF

Konteks:
Phase 9D sudah selesai secara implementasi.

Status terakhir:

AI DECISION INTEGRITY: PASS — PENDING NATURAL RESOLUTION LIVE PROOF

Alasan pending:
Saat validasi terakhir, database masih fresh:

* expired markets = 0
* future markets = 272
* outcome_learnings rows = 0
* direct_resolution = 0
* pnl_proxy = 0

Ini bukan bug. Market belum ada yang selesai.

TUGAS SEKARANG:
Lakukan natural resolution live proof setelah market Polymarket benar-benar expired/resolved.

JANGAN mulai dari nol.
JANGAN redo Phase 9B/9C/9D.
JANGAN ubah UI.
JANGAN redesign.
JANGAN tambah provider baru.
JANGAN tambah dummy data.
JANGAN tambah fake outcome.
JANGAN tambah assumed winner.
JANGAN pakai live probability sebagai resolved outcome.
JANGAN klaim PASS kalau direct_resolution masih 0.

==================================================
STEP 1 — CHECK CURRENT STATE

Baca file:

* AI_DECISION_INTEGRITY_AUDIT.md
* backend/app/services/outcome_learning_service.py
* backend/app/services/gamma_series_client.py
* backend/app/models/outcome_learning.py
* backend/app/repositories/outcome_learning_repository.py
* backend/app/core/database.py atau database.py sesuai struktur project

Lalu jalankan:

* git status
* python -m compileall -q backend/app

Pastikan Phase 9D code masih ada:

* OUTCOME_SOURCE_DIRECT
* OUTCOME_SOURCE_PROXY
* OUTCOME_SOURCE_NONE
* MarketResolutionResult
* fetch_market_resolution
* _parse_outcome_prices
* outcome_source column
* winning_side column
* winning_token_id column
* final_yes_price column
* final_no_price column
* resolution_note column

==================================================
STEP 2 — CHECK EXPIRED MARKETS

Query database:

* total market_universe rows
* count end_time < now
* count end_time > now
* expired markets with condition_id
* expired markets with yes_token_id / no_token_id
* expired markets already evaluated
* expired markets not yet evaluated

Jika masih:

expired markets = 0

Maka:

* jangan fabricate.
* tulis NO_RESOLVED_MARKET_AVAILABLE_YET.
* final status tetap PASS — PENDING NATURAL RESOLUTION LIVE PROOF.
* jangan coding ulang.

Jika expired markets > 0:
Lanjut Step 3.

==================================================
STEP 3 — RUN OUTCOME LEARNING CYCLE

Jalankan Outcome Learning cycle atau tunggu worker cycle.

Lalu cek log:

* expired_markets
* evaluated
* direct_resolution
* pnl_proxy
* skipped
* errors

Target ideal:

direct_resolution >= 1

Jika direct_resolution masih 0:

* cek apakah Gamma resolution tidak tersedia
* cek apakah market voided/cancelled
* cek apakah already_evaluated
* cek apakah no position / no decision
* jangan klaim direct proof

==================================================
STEP 4 — VERIFY GAMMA RAW RESOLUTION

Untuk minimal 1 market expired:

Ambil dari DB:

* condition_id
* question / title
* series_slug
* yes_token_id
* no_token_id
* end_time
* market status

Panggil Gamma existing integration.

Tampilkan raw relevant fields:

* conditionId
* question/title
* closed
* active
* archived
* outcomePrices
* clobTokenIds
* endDate
* condition_id match true/false
* token mapping true/false

Valid resolution hanya jika:

YES wins:

* closed=true
* outcomePrices YES >= 0.99
* outcomePrices NO <= 0.01

NO wins:

* closed=true
* outcomePrices YES <= 0.01
* outcomePrices NO >= 0.99

Void/cancel:

* outcomePrices [0,0]
* jangan hitung sebagai correct/incorrect trade prediction

Live probability:

* outcomePrices seperti 0.51/0.49
* jangan klaim resolved

==================================================
STEP 5 — VERIFY OUTCOME_LEARNINGS ROW

Query outcome_learnings terbaru.

Tampilkan:

* id
* condition_id
* prediction
* actual_outcome / winning_side
* correct
* outcome_source
* winning_token_id
* final_yes_price
* final_no_price
* resolution_note
* realized_pnl
* evaluated_at

Wajib:

* outcome_source = DIRECT_POLYMARKET_RESOLUTION
* winning_side = YES atau NO
* winning_token_id cocok dengan yes_token_id/no_token_id
* correct dihitung dari prediction vs winning_side
* realized_pnl hanya economic context, bukan primary correctness

Jika outcome_source = REALIZED_PNL_PROXY:

* dokumentasikan alasan direct resolution tidak tersedia.
* final status tetap PASS WITH CAVEATS atau PASS PENDING, bukan clean PASS.

==================================================
STEP 6 — VERIFY DOWNSTREAM EFFECT

Cek:

* EnginePerformance recompute setelah outcome row
* ConfidenceCalibration recompute jika ada
* DynamicWeight cycle berikutnya membaca outcome labeled source
* no duplicate outcome karena already_evaluated(condition_id)

Tampilkan minimal:

* engine_performance rows count
* engine_weights rows count
* last dynamic weight run if any
* apakah outcome_source label dipertahankan

==================================================
STEP 7 — RED FLAG SEARCH

Search backend:

* fake outcome
* synthetic outcome
* assumed outcome
* default winner
* hardcoded winner
* outcome_source
* winning_side
* direct_resolution
* pnl_proxy
* realized_pnl
* outcomePrices

Klasifikasikan:

* REAL_DATA
* DIRECT_POLYMARKET_RESOLUTION
* REALIZED_PNL_PROXY
* SAFE
* TEST_ONLY
* INVALID

Jika ada INVALID:

* perbaiki.
* final status FAIL sampai bersih.

==================================================
STEP 8 — UPDATE AUDIT REPORT

Update AI_DECISION_INTEGRITY_AUDIT.md.

Tambahkan sub-section:

PHASE 9D NATURAL RESOLUTION LIVE PROOF

Isi:

1. Market checked
2. DB market row
3. Gamma raw resolution evidence
4. OutcomeLearning row
5. direct_resolution counter
6. pnl_proxy counter
7. EnginePerformance / DynamicWeight downstream check
8. Red flag search result
9. Final status upgrade / no upgrade

Final status:

Jika direct_resolution >= 1 dan OutcomeLearning row valid:
AI DECISION INTEGRITY: PASS

Jika expired market belum ada:
AI DECISION INTEGRITY: PASS — PENDING NATURAL RESOLUTION LIVE PROOF

Jika only REALIZED_PNL_PROXY:
AI DECISION INTEGRITY: PASS WITH CAVEATS

Jika fake/default/assumed outcome ditemukan:
AI DECISION INTEGRITY: FAIL

==================================================
STEP 9 — FINAL RESPONSE FORMAT

Jawab dengan format:

1. Files read
2. Expired market count
3. Outcome learning cycle result
4. Market checked
5. Gamma raw resolution evidence
6. OutcomeLearning row
7. direct_resolution counter
8. pnl_proxy counter
9. Downstream effect
10. Red flag search
11. Audit report update
12. Final status
13. Exact next step

Jangan jawab umum.
Jangan fabricate market resolved.
Jangan klaim clean PASS kalau direct_resolution masih 0.